{
  "name": "spfx-with-any-versions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "build": "rm -rf ./temp/deploy && gulp bundle --ship && gulp package-solution --ship",
    "deploytoazure": "npm run build && gulp deploy-azure-storage",
    "clean": "gulp clean",
    "test": "gulp test",
    "serve": "gulp serve"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@microsoft/sp-build-web": "~1.1.0",
    "@microsoft/sp-module-interfaces": "~1.1.0",
    "@microsoft/sp-tslint-rules": "^1.8.2",
    "@microsoft/sp-webpart-workbench": "~1.1.0",
    "@types/chai": "3.4.34",
    "@types/mocha": "2.2.38",
    "ajv": "~5.2.2",
    "gulp": "~3.9.1",
    "source-map-loader": "0.2.4",
    "@types/node": "*"
  }
}
